--- a/networking/ntpd.c
+++ b/networking/ntpd.c
@@ -779,7 +779,7 @@ resolve_peer_hostname(peer_t *p, int loo
 //well... in which case, what is a good value for loop_on_fail?
 		//if (--loop_on_fail == 0)
 		//	xfunc_die();
-		sleep(5);
+		sleep(RETRY_INTERVAL);
 		goto again;
 	}
 	free(p->p_lsa);
